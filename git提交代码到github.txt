 git remote add orm https://github.com/ormorm/note.git
 git commit -m "我的注释"
 git add . git commit -m
 git push -u orm master

全部提交：
 git add .
 git commit -m "我的注释"
 git push -u orm master  第一次提交需要-u，以后可以不使用